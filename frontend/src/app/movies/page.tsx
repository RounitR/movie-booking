export default function MoviesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Movies</h1>
      <p className="text-sm text-gray-600 mb-2">This page will list movies from the backend.</p>
      <p>We will wire up fetching next, but the route and layout are ready.</p>
    </div>
  );
}